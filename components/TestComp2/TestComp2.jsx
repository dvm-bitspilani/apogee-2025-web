/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useEffect, useRef } from "react";
import { useGLTF, useAnimations } from "@react-three/drei";

export function TestComp2(props) {
  const group = useRef();
  const { nodes, materials, animations } = useGLTF("/models/LineAnim.glb");
  const { actions, names } = useAnimations(animations, group);

  useEffect(() => {
    actions[names[0]].play();
    console.log(names);
  }, []);
  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <lineSegments
          name="LINE"
          geometry={nodes.LINE.geometry}
          material={nodes.LINE.material}
          morphTargetDictionary={nodes.LINE.morphTargetDictionary}
          morphTargetInfluences={nodes.LINE.morphTargetInfluences}
        />
      </group>
    </group>
  );
}

useGLTF.preload("/models/LineAnim.glb");
